import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function ClientInboxPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Inbox</CardTitle>
        <CardDescription>View your consolidated inbox.</CardDescription>
      </CardHeader>
    </Card>
  );
}
