import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function AdminPackagesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Packages</CardTitle>
        <CardDescription>Manage your packages here.</CardDescription>
      </CardHeader>
    </Card>
  );
}
