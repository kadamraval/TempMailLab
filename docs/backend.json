
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the TempInbox application.",
      "properties": {
        "uid": { "type": "string", "description": "The user's unique ID from Firebase Auth." },
        "email": { "type": "string", "description": "User's email address.", "format": "email" },
        "displayName": { "type": "string", "description": "The user's display name." },
        "inboxCount": { "type": "number", "description": "The number of inboxes the user has." },
        "planId": { "type": "string", "description": "The ID of the user's current subscription plan." },
        "isAdmin": { "type": "boolean", "description": "Indicates if the user has administrative privileges." },
        "createdAt": { "type": "string", "description": "Timestamp of when the user account was created.", "format": "date-time" }
      },
      "required": ["uid", "email", "planId", "isAdmin", "createdAt"]
    },
    "Inbox": {
      "title": "Inbox",
      "type": "object",
      "description": "Represents a single temporary email inbox.",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who owns this inbox." },
        "emailAddress": { "type": "string", "description": "The temporary email address of the inbox." },
        "domain": { "type": "string", "description": "The domain part of the email address." },
        "emailCount": { "type": "number", "description": "The number of emails currently in the inbox." },
        "expiresAt": { "type": "string", "description": "Timestamp indicating when the inbox and its contents will be deleted.", "format": "date-time" },
        "isStarred": { "type": "boolean", "description": "If the user has starred this inbox." },
        "isArchived": { "type": "boolean", "description": "If the user has archived this inbox." },
        "createdAt": { "type": "string", "description": "Timestamp of when the inbox was created.", "format": "date-time" }
      },
      "required": ["userId", "emailAddress", "expiresAt", "createdAt"]
    },
    "Email": {
      "title": "Email",
      "type": "object",
      "description": "Represents a single email message within an inbox.",
      "properties": {
        "inboxId": { "type": "string", "description": "The ID of the parent inbox." },
        "userId": { "type": "string", "description": "Denormalized UID of the user for security rules." },
        "senderName": { "type": "string", "description": "Name of the sender." },
        "subject": { "type": "string", "description": "Subject line of the email." },
        "receivedAt": { "type": "string", "description": "Timestamp of when the email was received.", "format": "date-time" },
        "createdAt": { "type": "string", "description": "Timestamp of when the email was stored.", "format": "date-time" },
        "htmlContent": { "type": "string", "description": "HTML content of the email." },
        "textContent": { "type": "string", "description": "Plain text content of the email." },
        "rawContent": { "type": "string", "description": "The full, raw MIME content of the email." },
        "read": { "type": "boolean", "description": "Indicates if the email has been read by the user." },
        "isStarred": { "type": "boolean", "description": "Indicates if the user has starred the email." },
        "isArchived": { "type": "boolean", "description": "Indicates if the user has archived the email." },
        "isSpam": { "type": "boolean", "description": "Indicates if the user has marked the email as spam." },
        "isBlocked": { "type": "boolean", "description": "Indicates if the sender of the email is blocked." }
      },
      "required": ["inboxId", "userId", "senderName", "subject", "receivedAt", "createdAt"]
    },
    "Plan": {
      "title": "Plan",
      "description": "Defines a subscription plan and its features.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "planType": { "type": "string", "enum": ["guest", "freemium", "pro"] },
        "price": { "type": "number" },
        "billing": { "type": "string", "enum": ["monthly", "yearly", "lifetime_free"] },
        "status": { "type": "string", "enum": ["active", "archived"] },
        "features": { "type": "object" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "price", "billing", "status", "features"]
    },
    "BlogPost": {
        "title": "Blog Post",
        "type": "object",
        "description": "Represents a single blog post or article.",
        "properties": {
            "title": { "type": "string", "description": "The main title of the blog post." },
            "slug": { "type": "string", "description": "URL-friendly version of the title." },
            "content": { "type": "string", "description": "The full content of the blog post, usually in Markdown or HTML." },
            "excerpt": { "type": "string", "description": "A short summary of the post." },
            "featuredImage": { "type": "string", "description": "URL of the main image for the post.", "format": "uri" },
            "status": { "type": "string", "enum": ["draft", "published"], "description": "The publication status of the post." },
            "authorId": { "type": "string", "description": "The UID of the user who wrote the post." },
            "categoryId": { "type": "string", "description": "The ID of the primary category for the post." },
            "tags": { "type": "array", "items": { "type": "string" } },
            "publishedAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the post was published." },
            "createdAt": { "type": "string", "format": "date-time" },
            "updatedAt": { "type": "string", "format": "date-time" }
        },
        "required": ["title", "slug", "content", "status", "authorId"]
    },
    "Category": {
        "title": "Category",
        "type": "object",
        "description": "Represents a category for organizing blog posts.",
        "properties": {
            "name": { "type": "string", "description": "The name of the category." },
            "slug": { "type": "string", "description": "URL-friendly version of the name." },
            "description": { "type": "string", "description": "A short description of the category." },
            "postCount": { "type": "number", "description": "The number of posts in this category." }
        },
        "required": ["name", "slug"]
    },
    "Menu": {
      "title": "Menu",
      "type": "object",
      "description": "Represents a navigation menu.",
      "properties": {
        "id": { "type": "string" },
        "items": { 
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "label": { "type": "string" },
              "href": { "type": "string" },
              "order": { "type": "number" }
            }
          }
        }
      }
    }
  },
  "auth": {
    "providers": [ "password", "anonymous", "google.com" ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles."
    },
    "/inboxes/{inboxId}": {
      "schema": { "$ref": "#/entities/Inbox" },
      "description": "Global collection of all inboxes."
    },
    "/inboxes/{inboxId}/emails/{emailId}": {
      "schema": { "$ref": "#/entities/Email" },
      "description": "Sub-collection of emails for a given inbox."
    },
    "/plans/{planId}": {
      "schema": { "$ref": "#/entities/Plan" },
      "description": "Publicly readable subscription plans."
    },
    "/allowed_domains/{domainId}": {
      "schema": { "type": "object" },
      "description": "Domains available for generating inboxes."
    },
    "/admin_settings/{settingId}": {
      "schema": { "type": "object" },
      "description": "Settings configurable by admins."
    },
    "/posts/{postId}": {
      "schema": { "$ref": "#/entities/BlogPost" },
      "description": "Collection of all blog posts."
    },
    "/categories/{categoryId}": {
      "schema": { "$ref": "#/entities/Category" },
      "description": "Collection of all blog categories."
    },
    "/menus/{menuId}": {
      "schema": { "$ref": "#/entities/Menu" },
      "description": "Stores navigation menus (e.g., header, footer)."
    },
    "/pages/{pageId}": {
      "schema": { "type": "object", "properties": { "name": { "type": "string" }, "slug": { "type": "string" } } },
      "description": "Defines the pages of the website."
    },
    "/pages/{pageId}/sections/{sectionId}": {
      "schema": { "type": "object" },
      "description": "Stores the configuration for a section on a specific page."
    },
    "/sections/{sectionId}": {
        "schema": { "type": "object" },
        "description": "Stores default styles and configurations for reusable page sections."
    }
  }
}
